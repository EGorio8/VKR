shader_type spatial;

	void fragment() {
	// Устанавливаем альфа-значение пикселя в 0 для прозрачности
	// В зависимости от потребностей, вы можете настроить другие параметры цвета
	ALBEDO = vec3(1.0, 1.0, 1.0);
	ALPHA = 0.2;
}